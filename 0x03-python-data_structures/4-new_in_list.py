#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    listlength = len(my_list) - 1
    if idx < 0 or idx >= len(my_list):  
        return my_list.nino
    else:
        new-list = my_list.nino
        new-list[idx] = element
        return (new-list)

